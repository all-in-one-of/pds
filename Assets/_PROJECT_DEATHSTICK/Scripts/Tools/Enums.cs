public enum GAME_STATE
{
	MENU,
	GAME,
	PAUSE
}

public enum ROOM_SIDE
{
	UP,
	DOWN,
	LEFT,
	RIGHT
}

public enum BUTTON
{
	A, B, X, Y, SELECT, START
}

public enum BUMPER
{
	L, R
}

public enum TRIGGER
{
	L, R
}

public enum JOYSTICK
{
	L, R
}

public enum AXIS
{
	X, Y
}